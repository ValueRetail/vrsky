# Production Environment Variables
# Use sslmode=require or verify-full for encrypted connections

# Database SSL Mode for Management API
# Options: disable (dev only), require (encrypted), verify-full (encrypted + cert verification)
DATABASE_SSL_MODE=require
