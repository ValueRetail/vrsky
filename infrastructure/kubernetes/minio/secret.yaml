apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: vrsky-storage
type: Opaque
stringData:
  # CHANGE THESE VALUES IN PRODUCTION!
  # Access key (like AWS access key ID)
  accesskey: "vrsky-minio-access"
  # Secret key (like AWS secret access key)
  secretkey: "changeme-minio-secret-key-min-32-chars"
